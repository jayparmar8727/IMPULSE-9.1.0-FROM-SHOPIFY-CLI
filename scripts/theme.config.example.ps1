# Kansawala — theme-dev config (TEMPLATE)
# -----------------------------------------------------------------------------
# Copy this file to  scripts/theme.config.ps1  and fill in your values.
# theme.config.ps1 is gitignored (machine-local) — never commit it.
#
# How to get the theme ID: see docs/THEME-DEV-WORKFLOW.md (Setup).

$Store = "kansawalasmf"

# Your PERSISTENT dev theme ID — an UNPUBLISHED theme in the Theme library,
# NOT the throwaway theme that `shopify theme dev` creates (that one auto-deletes).
$ThemeId = "PASTE-PERSISTENT-THEME-ID-HERE"
